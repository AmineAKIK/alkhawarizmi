import { prodNode, prodSheet } from "./production-common";

export const productionPerformance = prodSheet({
  id: "production-performance",
  number: 3,
  title: "Performance et Optimisation",
  subtitle: "Mesurer les vrais goulots, optimiser là où ça compte, scaler seulement quand le système le demande.",
  badge: "Fiche P03",
  meta: ["5 nœuds"],
  readingTime: "40 min",
  description: "Profiling, cache, base de données, frontend et scaling pour améliorer la performance sans complexifier à l'aveugle.",
  accent: "performance",
  nodes: {
    mesureAvantOptimiser: prodNode({
      id: "mesureAvantOptimiser",
      label: "Mesurer avant d'optimiser",
      icon: "🎯",
      kind: "performance",
      niveau: "Fondation",
      why: "Optimiser sans mesurer revient à résoudre un problème qu'on n'a pas identifié. Le résultat est souvent du code plus complexe et aucun gain utilisateur.",
      system: "La mesure précède le cache, l'optimisation BDD, l'optimisation frontend et le scaling. Elle s'appuie sur métriques, profiling et traces.",
      choice: "APM pour le backend, EXPLAIN ANALYZE pour la base de données, Lighthouse et Web Vitals pour le frontend. Chaque couche a son outil de vérité.",
      senior: "Il identifie le goulot d'étranglement avant d'agir. Dans beaucoup d'apps, 80% du temps est consommé par 20% du système: requêtes BDD, appels externes ou JS bloquant.",
      errors: `<p><strong>Pattern 1 — Optimiser sans profiler :</strong> réécrire une fonction alors que la lenteur vient d'une requête SQL.</p><p><strong>Pattern 2 — Mauvaise métrique :</strong> gagner 50ms serveur sans améliorer la performance perçue.</p><p><strong>Pattern 3 — Micro-optimisation :</strong> économiser des microsecondes pendant qu'une requête prend 2 secondes.</p>`,
      invariants: `<p><strong>Ce qui change :</strong> les profilers et métriques. <strong>Ce qui ne change pas :</strong> on ne sait pas où optimiser avant d'avoir mesuré.</p>`,
      practice: {
        kind: "cmds",
        commands: [
          { type: "comment", value: "PostgreSQL — analyser une requête" },
          { type: "cmd", value: "EXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 1;" },
          { type: "comment", value: "Node.js — profiler" },
          { type: "cmd", value: "node --prof src/index.js" },
          { type: "cmd", value: "node --prof-process isolate-*.log > profile.txt" },
          { type: "comment", value: "Frontend" },
          { type: "cmd", value: "npx lighthouse https://votre-app.com --output json" },
        ],
        debt: "Optimiser sans benchmark avant/après = complexité sans preuve de bénéfice.",
      },
      verification: [
        "Pourquoi l'optimisation prématurée crée-t-elle de la dette ?",
        "Quelles sont tes 3 premières étapes face à un endpoint lent ?",
        "Différencie performance serveur et performance perçue.",
      ],
    }),
    cache: prodNode({
      id: "cache",
      label: "Cache",
      icon: "⚡",
      kind: "performance",
      niveau: "Intermédiaire",
      why: "La requête la plus rapide est celle qu'on ne fait pas. Le cache réutilise le résultat d'une opération coûteuse pour réduire latence et charge.",
      system: "Le cache se place entre l'application, la base de données, les APIs externes et l'utilisateur. Il est souvent le premier levier avant le scaling.",
      choice: "Cache HTTP et CDN pour assets, Redis pour cache applicatif, cache de requêtes et pooling côté base. Chaque niveau a sa fraîcheur et son coût.",
      senior: "Il pense l'invalidation avant le remplissage. Il anticipe aussi le thundering herd: quand un cache expire, toutes les requêtes ne doivent pas frapper la BDD ensemble.",
      errors: `<p><strong>Pattern 1 — Pas d'invalidation :</strong> l'utilisateur voit des données périmées après update.</p><p><strong>Pattern 2 — Cacher les erreurs :</strong> une panne temporaire devient persistante en cache.</p><p><strong>Pattern 3 — Masquer un vrai bug :</strong> cacher une requête N+1 au lieu de la corriger.</p>`,
      invariants: `<p><strong>Ce qui change :</strong> Redis, CDN, edge et navigateurs. <strong>Ce qui ne change pas :</strong> tout cache échange fraîcheur contre vitesse.</p>`,
      practice: {
        kind: "cmds",
        commands: [
          { type: "comment", value: "Redis — cache-aside" },
          { type: "cmd", value: "npm install ioredis" },
          { type: "snippet", value: "const cached = await redis.get(`user:${id}`)" },
          { type: "snippet", value: "if (cached) return JSON.parse(cached)" },
          { type: "snippet", value: "const user = await db.user.findUnique({ where: { id } })" },
          { type: "snippet", value: "await redis.setex(`user:${id}`, 3600, JSON.stringify(user))" },
          { type: "snippet", value: "await redis.del(`user:${id}`)  // après update" },
        ],
        debt: "Cache sans invalidation = données périmées exposées aux utilisateurs.",
      },
      verification: [
        "Explique le pattern cache-aside.",
        "Qu'est-ce que le thundering herd ?",
        "Pourquoi ne pas cacher pour masquer une requête mal conçue ?",
      ],
    }),
    performanceBDD: prodNode({
      id: "performanceBDD",
      label: "Performance base de données",
      icon: "🗄",
      kind: "performance",
      niveau: "Intermédiaire",
      why: "La base est souvent le goulot principal. N+1, index manquants et connexions non poolées sont invisibles avec peu de données et catastrophiques à l'échelle.",
      system: "La performance BDD dépend de l'ORM, du schéma, des index, du cache, du monitoring et du logging des requêtes lentes.",
      choice: "Prioriser les index, éliminer les N+1, utiliser le connection pooling et paginer toutes les grandes listes.",
      senior: "Il active le slow query log et vérifie les plans d'exécution. Les index vont sur les colonnes utilisées dans WHERE, JOIN et ORDER BY, puis il supprime les index inutilisés.",
      errors: `<p><strong>Pattern 1 — N+1 ignoré :</strong> 1 liste + N auteurs = explosion de requêtes.</p><p><strong>Pattern 2 — SELECT * partout :</strong> transférer des colonnes inutiles coûte cher.</p><p><strong>Pattern 3 — Foreign key sans index :</strong> les filtres fréquents deviennent des full scans.</p>`,
      invariants: `<p><strong>Ce qui change :</strong> ORM et moteurs SQL. <strong>Ce qui ne change pas :</strong> index, cardinalité, N+1 et pagination restent des réalités algorithmiques.</p>`,
      practice: {
        kind: "cmds",
        commands: [
          { type: "comment", value: "PostgreSQL — slow queries" },
          { type: "cmd", value: "ALTER SYSTEM SET log_min_duration_statement = 100;" },
          { type: "cmd", value: "SELECT pg_reload_conf();" },
          { type: "comment", value: "ALTER SYSTEM nécessite des droits élevés; sur base managée, passer par la configuration plateforme" },
          { type: "comment", value: "Analyser et indexer" },
          { type: "cmd", value: "EXPLAIN ANALYZE SELECT * FROM orders WHERE user_id = 1;" },
          { type: "cmd", value: "CREATE INDEX CONCURRENTLY idx_orders_user_id ON orders(user_id);" },
          { type: "comment", value: "CREATE INDEX CONCURRENTLY ne s'exécute pas dans une transaction" },
          { type: "snippet", value: "prisma.post.findMany({ include: { author: true } })" },
        ],
        debt: "N+1 non détecté en dev = incident de performance en production.",
      },
      verification: [
        "Explique un N+1 et son correctif.",
        "Comment vérifier qu'un index est utilisé ?",
        "Pourquoi 10 lignes en dev masquent les problèmes de prod ?",
      ],
    }),
    performanceFrontend: prodNode({
      id: "performanceFrontend",
      label: "Performance frontend",
      icon: "🖥",
      kind: "performance",
      niveau: "Intermédiaire",
      why: "La performance perçue dépend fortement du navigateur: bundle JS, images, fonts, rendu initial et stabilité visuelle.",
      system: "Elle relie rendu, responsive, cache HTTP, CDN et choix de framework. Un serveur rapide ne compense pas un bundle énorme.",
      choice: "Suivre les Core Web Vitals actuels: LCP pour l'affichage principal (< 2.5s bon), INP pour la réactivité (< 200ms bon, remplace FID depuis mars 2024), CLS pour la stabilité visuelle (< 0.1 bon).",
      senior: "Il réduit le chemin critique: HTML utile, CSS minimal, JS différé, images dimensionnées. Afficher vite quelque chose de stable vaut souvent mieux que tout charger avant le premier rendu.",
      errors: `<p><strong>Pattern 1 — Bundle monolithique :</strong> tout le JS est requis pour la première page.</p><p><strong>Pattern 2 — Images non optimisées :</strong> une image de 5MB devient une miniature.</p><p><strong>Pattern 3 — Fonts bloquantes :</strong> le texte reste invisible sans font-display swap.</p>`,
      invariants: `<p><strong>Ce qui change :</strong> bundlers, formats et navigateurs. <strong>Ce qui ne change pas :</strong> chaque octet et chaque milliseconde CPU dans le navigateur coûtent à l'utilisateur.</p>`,
      practice: {
        kind: "cmds",
        commands: [
          { type: "comment", value: "Mesurer" },
          { type: "cmd", value: "npx lighthouse https://votre-app.com --view" },
          { type: "cmd", value: "npx vite-bundle-visualizer --help" },
          { type: "comment", value: "Optimiser images et chargement" },
          { type: "cmd", value: "npm install --save-dev sharp" },
          { type: "snippet", value: '<img src="photo.webp" loading="lazy" width="800" height="600">' },
          { type: "snippet", value: "const Dashboard = lazy(() => import('./Dashboard'))" },
        ],
        debt: "Bundle jamais analysé = dead code livré aux utilisateurs.",
      },
      verification: [
        "Quels sont les Core Web Vitals ?",
        "Quelle première action face à un bundle de 3MB ?",
        "Pourquoi font-display: swap aide la perception ?",
      ],
    }),
    scaling: prodNode({
      id: "scaling",
      label: "Scaling",
      icon: "📈",
      kind: "processus",
      niveau: "Avancé",
      why: "Le scaling ne s'improvise pas pendant un pic. Il se prépare en comprenant les limites, les goulots et l'état partagé du système.",
      system: "Le scaling s'appuie d'abord sur cache, optimisation BDD et architecture stateless. Il touche ensuite load balancing, replicas, CDN et orchestration.",
      choice: "Scaling vertical pour simplicité, horizontal pour redondance et croissance, database scaling pour lecture/partitionnement, CDN pour proximité géographique.",
      senior: "Il sait qu'une app stateful ne scale pas horizontalement proprement. Sessions en Redis, fichiers dans object storage, état partagé hors mémoire locale.",
      errors: `<p><strong>Pattern 1 — App stateful :</strong> deux serveurs ne partagent pas les sessions mémoire.</p><p><strong>Pattern 2 — Scaler avant optimiser :</strong> ajouter des serveurs amplifie les requêtes inefficaces.</p><p><strong>Pattern 3 — Sur-architecture :</strong> Kubernetes et microservices avant d'avoir le problème.</p>`,
      invariants: `<p><strong>Ce qui change :</strong> plateformes et outils d'orchestration. <strong>Ce qui ne change pas :</strong> la capacité globale est déterminée par le vrai goulot d'étranglement.</p>`,
      practice: {
        kind: "exercices",
        titre: "Évaluer le potentiel de scaling",
        etapes: [
          "Identifie si ton app garde de l'état en mémoire ou sur disque local.",
          "Liste ce qui doit être externalisé: sessions, fichiers, jobs, cache.",
          "Identifie le goulot actuel: CPU, mémoire, DB, connexions ou requêtes.",
          "Définis un seuil de trafic à partir duquel scaler sera envisagé.",
        ],
        output: "Diagnostic stateful/stateless, plan d'externalisation, goulot et seuil de scaling.",
        critere: "Si l'app est stateful, l'externalisation de l'état précède le scaling horizontal.",
        piege: "Confondre scalable et compliqué. Une app simple peut scaler très loin verticalement.",
      },
      verification: [
        "Différence entre scaling vertical et horizontal ?",
        "Pourquoi le stateful bloque le horizontal scaling ?",
        "Pourquoi scaler avant optimiser est une erreur ?",
      ],
    }),
  },
  maps: {
    universel: {
      viewBox: "0 0 820 270",
      nodes: [
        { id: "mesureAvantOptimiser", x: 10, y: 100, w: 190, h: 65 },
        { id: "cache", x: 250, y: 100, w: 100, h: 65 },
        { id: "performanceBDD", x: 400, y: 40, w: 170, h: 65 },
        { id: "performanceFrontend", x: 400, y: 165, w: 175, h: 65 },
        { id: "scaling", x: 640, y: 100, w: 155, h: 65 },
      ],
      edges: [
        { x1: 200, y1: 132, x2: 248, y2: 132, label: "révèle" },
        { x1: 350, y1: 112, x2: 398, y2: 72, label: "réduit" },
        { x1: 350, y1: 150, x2: 398, y2: 188, label: "optimise" },
        { x1: 570, y1: 73, x2: 638, y2: 118, label: "permet" },
        { x1: 575, y1: 200, x2: 638, y2: 152, label: "permet" },
      ],
    },
  },
});
